# Environment and secrets
.env
.venv/

# Python artifacts
__pycache__/

# macOS artifacts
.DS_Store

# data lake — never in git
data/
*.duckdb
*.duckdb.wal
*.parquet

# Python scratch files
scratch.py

# backfill run logs are under data/, which is already ignored

# notes and design docs are not codebase artifacts
CLAUDE.md
docs/

# notebook scratch state, not the notebook itself
.ipynb_checkpoints/

# dbt build artifacts. Generated on every run.
transform/target/
transform/logs/
transform/dbt_packages/
transform/.user.yml
